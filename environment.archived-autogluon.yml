name: sa-aki-archived-autogluon-070
channels:
  - conda-forge
dependencies:
  - python=3.9
  - pip
  - pip:
      - autogluon.tabular==0.7.0
      - scikit-learn==1.2.2
      - xgboost==1.7.4

# This environment is only for replaying the frozen submitted classifier.
# It is separate from the current development dependencies in pyproject.toml.
